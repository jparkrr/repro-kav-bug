# Repro React-Freeze bug with KeyboardAvoidingView

To run:

```
# clone
git clone git@github.com:jparkrr/repro-kav-bug.git
cd repro-kav-bug

# install deps
yarn
npx pod-install

# compile and run
yarn ios
```
